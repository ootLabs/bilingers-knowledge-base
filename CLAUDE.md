# Bilingers

Project instructions live in `AGENTS.md` - one file, shared by every agent tool, so the rules cannot drift apart. It is imported below and loads automatically with this file.

@AGENTS.md

Do not copy rules into this file. Change `AGENTS.md` instead.
